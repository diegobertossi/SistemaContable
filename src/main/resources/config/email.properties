# Configuración SMTP para FacturaSoft
# Editar con los datos del servidor SMTP

smtp.host=smtp.gmail.com
smtp.port=587
smtp.user=
smtp.pass=
smtp.auth=true
smtp.starttls=true

# Para Gmail: usar cuenta completa @gmail.com y App Password (no contraseña normal)
# Para otros servidores: ajustar host, port y credenciales correspondientemente